import React from 'react';

export default function(ct,  h = null, m = null, op = null) {

};

// functions that determin each phase:
// Is ct in Inactive time?
// Is ct in Active time?
// Is ct in Active time2?

  // if any are true, render differnt things.




// // determin what I'm tryna change
// // swap out the time with what was given
// let hIndex = ct.indexOf(':'); // 00:00:00 _M
// let mIndex = hIndex + 3;
// let sIndex = mIndex + 3;
// let time;
// if (h) {
//   if (op === 'add');
//   time = ct.slice(hIndex, ct.length - 1); // cut the hour.
//   time = `${h}${time}`;
// }